import { courtsideNavigation } from './courtside-navigation';

describe('courtsideNavigation', () => {
  it('should work', () => {
    expect(courtsideNavigation()).toEqual('courtside-navigation');
  });
});
